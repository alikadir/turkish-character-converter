name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: surge deploy :)
      run: surge --domain $DOMAIN
      env:
        DOMAIN: ${{ secrets.SURGE_DOMAIN }} 
          
    
